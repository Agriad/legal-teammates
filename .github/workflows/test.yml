on: 
  issues:
    types: [opened]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: testing
    steps:
    - name: Hello world action step
      id: hello
      uses: ./
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
